node_modules/
dist/
target/
*.node
.DS_Store
.env
.env.*
packages/native/index.js
packages/native/index.d.ts

# Large local artifacts that must never enter git history
*.iso
*.aria2

# Isolated worktrees created for parallel agent runs; never part of the tree
.claude/worktrees/
