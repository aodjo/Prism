node_modules/
dist/
target/
out/
*.node
.DS_Store
.env
.env.*
# The one that is a template rather than a secret, and the only way somebody knows what to fill
# in. Named after the pattern above it so the exception has to be stated.
!deploy/rendezvous/.env.example
packages/native/index.js
packages/native/index.d.ts

# Large local artifacts that must never enter git history
*.iso
*.aria2

# Isolated worktrees created for parallel agent runs; never part of the tree
.claude/worktrees/

# Bundled from each application's sources, next to the markup that loads them.
packages/host/renderer/panel.js
packages/host/renderer/panel.js.map
packages/host/renderer/prism.css
packages/client/renderer/panel.js
packages/client/renderer/panel.js.map
packages/client/renderer/setup.js
packages/client/renderer/setup.js.map
packages/client/renderer/home.js
packages/client/renderer/home.js.map
packages/client/renderer/bridge.js
packages/client/renderer/bridge.js.map
packages/client/renderer/prism.css

# Scratch snapshots and logs from browser automation, never part of the tree.
.playwright-mcp/

# Written by tauri-build from tauri.conf.json and the capabilities beside it.
crates/prism-tauri/gen/
crates/prism-tauri/sidecar/
