{
  "name": "prism",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "client": "pnpm run build && tauri dev",
    "client:setup": "pnpm run build && PRISM_WINDOW_PAGE=setup.html tauri dev",
    "dev": "pnpm run client",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check",
    "lint:cross": "cargo clippy --workspace --exclude prism-rendezvous --exclude prism-tauri --all-targets --target x86_64-unknown-linux-gnu -- -D warnings && cargo clippy --workspace --exclude prism-rendezvous --exclude prism-tauri --exclude prism-cli --exclude prism-stream --all-targets --target x86_64-pc-windows-msvc -- -D warnings",
    "package": "node scripts/version.mjs && pnpm run build && tauri build",
    "test": "pnpm -r test && pnpm run typecheck && cargo test --workspace",
    "test:rs": "cargo test --workspace",
    "test:ts": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "publish-local": "node scripts/publish-local.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@11.18.0"
}
